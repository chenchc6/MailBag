// A literal object with a serverAddress property that gives the address of the MailBag server
export const config: { serverAddress: string, userEmail: string } =
{
  serverAddress : "http://localhost",
  userEmail : "monniechuang@outlook.com"
};
